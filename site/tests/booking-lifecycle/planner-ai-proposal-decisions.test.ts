import assert from 'node:assert/strict';import test from 'node:test';import {validateAiProposalDecision} from '../../src/lib/planner/ai-proposal-decisions.ts';
const id='10000000-0000-4000-8000-000000000001';const item={title:'Walk',type:'activity',description:'',startTime:null,endTime:null,location:null,status:'proposed'};const proposal={format:'olrig-holiday-plan-proposal',version:'1.0',planId:id,sourceRevision:2,summary:'Changes',operations:[{op:'add_item',dayId:id,afterItemId:null,item},{op:'remove_item',itemId:id}]};
test('validates rejection and selective acceptance',()=>{assert.equal(validateAiProposalDecision({action:'reject',reason:'Not suitable'},proposal).valid,true);assert.equal(validateAiProposalDecision({action:'accept',selections:[{operationIndex:1}]},proposal).valid,true)});
test('rejects duplicate indexes and protected edited operations',()=>{assert.equal(validateAiProposalDecision({action:'accept',selections:[{operationIndex:0},{operationIndex:0}]},proposal).valid,false);assert.equal(validateAiProposalDecision({action:'accept',selections:[{operationIndex:0,replacement:{...proposal.operations[0],item:{...item,status:'booked'}}}]},proposal).valid,false)});
